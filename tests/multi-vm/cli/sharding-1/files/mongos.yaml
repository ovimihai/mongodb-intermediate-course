sharding:
  configDB: csrs/${HOSTNAME}:26001,${HOSTNAME}:26002,${HOSTNAME}:26003
net:
  bindIp: 0.0.0.0
  port: 26000
systemLog:
  destination: file
  path: /var/mongodb/db/mongos.log
  logAppend: true
processManagement:
  fork: true